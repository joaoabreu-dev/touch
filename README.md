# Touch

A Windows version of the Linux `touch` command.

When working in the Windows terminal, one of the tools I miss the most is `touch`.
Windows has alternatives like `type nul > file.txt`, but they are not as simple or intuitive as typing:

```bash
touch file.txt
